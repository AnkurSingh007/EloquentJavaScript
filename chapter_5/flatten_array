let arrays = [[1, 2, 3], [4, 5], [6]];
// accumulator is the initial value 
// current is the current element of the array being processed
function flatten_array(arrays){
  return arrays.reduce((accumulator, current) => accumulator.concat(current), []);
}
console.log(flatten_array(arrays));
