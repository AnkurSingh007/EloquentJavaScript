function range(start, end){
  list  = []
  for(let i = start; i <= end; i++){
    list.push(i);
  }
  return list;
}

function sum(list){
  let sum = 0;
  for(let ele of list){
    sum += ele;
  }
  return sum;
}

console.log(range(1, 100));
console.log(sum(range(1, 50)));
